# Flyway off + H2: slices like @DataJpaTest can use ddl-auto. Full SQL migrations (e.g. GIN) need PostgreSQL — see integration tests.
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  flyway:
    enabled: false
  thymeleaf:
    cache: false

logging:
  level:
    root: WARN
    org.springframework: WARN
    com.recicar.marketplace: INFO

